{
  "name": "highlight-filesystem-mcp",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node ./dist/index.js",
    "dev": "node ./src/index.js",
    "build": "mkdir -p dist && cp -R src/* dist/"
  },
  "dependencies": {
    "@anaisbetts/mcp-installer": "^0.5.0",
    "@modelcontextprotocol/server-github": "^0.6.2",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
